// ltl-basic-interp/main.ltl
//
// Dartmouth BASIC interpreter — line numbers, GOSUB, PRINT.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-basic-interp v0.1.0")
    io::println("Dartmouth BASIC interpreter — line numbers, GOSUB, PRINT.")
    0
}
